#!/bin/bash
DIR=$1
set -x
mktorrent -v \
  -a wss://tracker.webtorrent.dev:443,wss://tracker.openwebtorrent.com:443,wss://open.ftorrent.com:443,wss://tracker.btorrent.xyz:443 \
  -o $(basename "$DIR").torrent \
  "$DIR"
